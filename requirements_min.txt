# ─────────────────────────────────────────────────────────────────
# SONYA — минимальный набор для первого GPU-теста
# Только YOLO + видео + метрики. Без Whisper/Audio.
# Установка: pip install -r requirements_min.txt
# Время: ~2–5 мин на Vast.ai
# ─────────────────────────────────────────────────────────────────

# --- YOLO детекция ---
ultralytics>=8.1.0

# --- Видео ---
opencv-python>=4.8.1
ffmpeg-python>=0.2.0
moviepy>=1.0.3

# --- Данные и утилиты ---
pandas>=2.1.0
numpy>=1.24.0
tqdm>=4.66.0
loguru>=0.7.0
psutil>=5.9.0
PyYAML>=6.0

# --- GPU мониторинг ---
nvidia-ml-py>=12.535.0

# --- Прочее ---
python-dotenv>=1.0.0
scipy>=1.11.0
